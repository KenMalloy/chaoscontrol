# Layer 1: Random gate control — fires at 10% rate randomly, same compute as surprise-gated
# Tests whether surprise TIMING matters or just extra compute
model_type: ssm
model_dim: 128
num_layers: 4
a_mode: diag
metabolic_gate: true
metabolic_k: 4
metabolic_mode: mcts
mcts_horizon: 8
metabolic_threshold: 0.1
metabolic_threshold_mode: random
