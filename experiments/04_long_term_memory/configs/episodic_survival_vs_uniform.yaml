# Tests survival scoring vs uniform merge for episodic compression.
# Requires code modification — survival scoring flag not yet wired.
a_mode: full
rich_b_mode: hub
outer_model_dim: 64
outer_model_type: multislot
consolidation_trigger: resolution
consolidation_mode: symmetric
model_dim: 128
num_layers: 4
